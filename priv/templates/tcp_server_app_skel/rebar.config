{erl_opts, [debug_info]}.

{deps, [
  {tcp_server, ".*", {git, "git://github.com/cooldaemon/tcp_server.git", ""}}
]}.

{cover_enabled, true}.

{clean_files, [
  "logs/*",
  "test/*.beam",
  "doc/*",
  "erl_crash.dump"
]}.
