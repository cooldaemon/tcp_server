{erl_opts, [
  debug_info,
  {i, "deps/tcp_server/include"}
]}.

{deps, [
  {tcp_server, ".*", {git, "git://github.com/cooldaemon/tcp_server.git", "master"}}
]}.

{cover_enabled, true}.

{clean_files, [
  "logs/*",
  "test/*.beam",
  "doc/*",
  "erl_crash.dump"
]}.
